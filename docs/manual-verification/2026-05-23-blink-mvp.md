# Blink MVP Manual Verification

Date: 2026-05-23

## Automated Checks

- `rtk swift test`
  - Result: pass.
  - Coverage: 26 XCTest tests, 0 failures.
- `rtk swift build`
  - Result: pass.
- `rtk ./scripts/package-app.sh debug`
  - Result: pass.
  - Evidence: generated `.build/Blink.app`.
- `rtk proxy sh -c 'swift run Blink >/tmp/blink-run.log 2>&1 & pid=$!; sleep 3; kill $pid 2>/dev/null || true; wait $pid 2>/dev/null || true; cat /tmp/blink-run.log'`
  - Result: pass.
  - Evidence: product built and the process started without startup errors during the smoke window.
- `.build/Blink.app --show-launcher`
  - Result: partial pass.
  - Evidence: process starts, `--show-launcher` path logs, and `CGWindowListCopyWindowInfo(.optionAll, ...)` reports Blink layer-0 windows at visible-size bounds, including a launcher-sized window around `X=80 Y=150 W=680 H=452`.
  - Limit: `CGWindowListCopyWindowInfo(.optionOnScreenOnly, ...)`, Computer Use, and `screencapture` do not provide reliable visual confirmation in this automation session.

## User Journey Status

- Option-Space shows and hides the launcher: not fully verified interactively in this run.
- Typing `now` shows timestamp results: verified once through the app window and covered by `TimestampProviderTests`.
- Enter on timestamp result reports copy success: verified once through the app window and `pbpaste`; covered by `TimestampProviderTests`.
- Text clipboard entries persist and search: covered by `ClipboardRepositoryTests` and `ClipboardProviderTests`.
- Image clipboard entries persist within cache limits: capture path builds; cache persistence is represented by metadata and retention tests.
- File URL clipboard entries can be represented: capture path builds; provider execution needs broader manual Finder validation.
- File reveal/open safety: file validation covered by `FileActionServiceTests`; Finder reveal not fully verified interactively.
- Clear history removes database rows and search index: covered by `ClipboardRepositoryTests`.
- Pausing recording prevents new clipboard entries: menu toggle is implemented and persisted through `settings.json`; full interactive verification still needs a foreground desktop run.

## Latest Foreground-Automation Attempt

- Started `.build/Blink.app --show-launcher`.
- Confirmed process arguments include `--show-launcher`.
- Confirmed diagnostics log contains `Showing launcher from --show-launcher`.
- `osascript` key-event path failed because macOS denied synthetic key sending.
- `CGEvent` synthetic `Option-Space` did not produce a launcher window, which is inconclusive because this environment may also block or drop synthetic HID events.
- Computer Use still reports `cgWindowNotFound` for Blink even when CGWindow reports Blink-owned windows.
- `screencapture` failed with `could not create image from display`, so this session does not have a reliable screen-capture permission path.

## Notes

This verification confirms the package compiles, the command/storage/provider core is covered by tests, the app can be packaged as `.build/Blink.app`, and the launcher window is created as a visible CGWindow. Computer Use intermittently fails to attach to the Blink accessibility tree even when CGWindow reports the window; global hotkey behavior, menu clicks, and real Finder interactions still need foreground manual validation.
